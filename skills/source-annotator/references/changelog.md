# 版本演进（从 SKILL.md 正文下沉的历史记录，只增不改）

- **v1.25（2026-09-12，实战驱动修订）**：格式卡顶部新增「标准形状」对照（行尾标点/括注跨行/箭头悬行尾——四批输入系统标注中每批 10-20 处返工的三大来源，按"示例优于规则"处置）；"伤疤"术语与 16 标签显式映射（ADR 0001）；正文删 `（v1.22 新增）` 式元标记，历史下沉本文件；跨层签名论断强制回查（§6 语义三问，事故案例 addToDisplay 出参）；WARN 处置口径成文；知识库存量基线问题处置规则（§4）；脚本 v2.2：第 10 项专名回查（搜已提交树防批注自证）+ `--mirror` 副本校验；权威源定为 `~/.agents`（docs/MAINTENANCE.md）。
- **v1.24 及以前**：见 git 历史（Summary/skills/source-annotator 仓库）。要点：v1.22 定位升级为"学习产出"、两面讲解 + 使用示例、SDK 启示触发点放宽到每个新类、关键机制大篇幅授权；行宽由 v1.20 的固定 130 列改为"所在文件 HEAD 既有行包络（下限 130）"自适应——上游注释宽度各库不一，统一规范不如跟随宿主文件。
- **v1.26（2026-09-13，内容质量门）**：用户裁定 v1.25 只修了机械层，注释内容生成质量未动。对齐 language README「问题式笔记质量门」时二次返工——初版"问句化改写"被否（问句开场用黑话、删骨架地图、长因果链塞三件事），修正为总纲"先问对问题，再平实作答"。落地：SKILL.md §3.0 新增内容质量门（六约束+优先级+三块型）与写前三步；新建 references/annotation-writing-guide.md（读者画像/12 阻力点/块分三种/六约束 before-after/五模板/三测，示例全部取自四批真实注释）；InputListener 为回炉样张（接入示例/深拷贝 why/结论先行）。样张炸出脚本与格式卡的文档不一致并修正（v2.2）：第 7 项行尾标点此前对纯 ASCII 新增行也检查，与「使用示例块」承诺矛盾——改为只查含真汉字的中文批注行。
- **v1.26 补遗（2026-09-13，验收意见驱动）**：新增表达细则六则入写作指南 §6 与 SKILL.md 内容质量门——列表优先（可枚举必列表，分号串列为反模式信号）、换行美观（续行对齐/相邻项长度均匀）、术语一致、主动语态、主旨自足+细节指针、数字带单位与出处；格式卡「列表化优先」补美观细则。用户验收意见：改写不得牺牲简单易懂，骨架地图块合法。
- **v1.26 补遗二（2026-09-13，执行提速）**：十处样张改写跑了 6 轮校验（每轮付一次全仓 grep），根因是写后未自查 + 格式迭代轮陪跑专名回查。落地三措施：指南 §9 增"写后自查"（标点/括号/占位词，自查通过才跑机械门）；SKILL.md 机械门补 `--fast` 说明与"十项"计数勘误；脚本 v2.3 `--fast` + 专名缓存（仓库+HEAD 级，只缓存存在结果，HEAD 变化自动失效）。
