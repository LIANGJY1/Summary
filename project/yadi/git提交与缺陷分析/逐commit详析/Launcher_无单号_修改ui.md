# 无单号 · 3D 车模 UI 调整（kanzi AAR 更新）

- **提交**：`7e5debd5` | 2026-09-18 | liqingqing | Launcher | bugfix（标注：纯 AAR 二进制更新）
- **缺陷库**：未关联单号（标题中单号为 SIR-***）

## 问题
3D 车模 UI 需要调整（提交标题 [SIR-***][YD_CCU][3D车模]修改ui）。

## 根因分析
不适用：本提交为纯二进制依赖更新，无可剖析源码 diff。3D 车模由 Kanzi 渲染引擎产出，UI 修改在 Kanzi 工程侧完成后打包为 AAR 替换。

## 关键代码修改
改动文件：application/Launcher/libs/kanzi-release.aar（二进制更新，400,895,959 → 428,286,927 字节，约 +26MB）
```diff
- kanzi-release.aar (Bin 400895959 -> 428286927 bytes)
```

## 为什么能修复
Kanzi 侧工程内容随 AAR 重新集成，UI 表现随之更新；应用层代码无变化。体量增长约 26MB，推测包含新贴图/模型资源，对包体积与加载内存的影响需关注。

## 复盘与经验
- 3D 车模类 UI 迭代走"Kanzi 工程出 AAR → 应用仓库换包"的链路，应用仓库内只能看到二进制差异，排障要回到 Kanzi 工程与 AAR 版本对应关系。
- 大体积 AAR（本次超 400MB）建议在提交信息中标注版本号/来源，否则事后无法追溯包的对应关系。
