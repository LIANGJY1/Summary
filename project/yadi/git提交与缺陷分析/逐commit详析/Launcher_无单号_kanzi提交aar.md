# 无单号 · kanzi 3D 车模 aar 更新
- **提交**：`a10f252c` | 2026-09-14 | liqingqing | Launcher | 二进制依赖更新（特殊类型：纯 aar 更新）
- **缺陷库**：未关联单号（占位单号 SIR-***）

## 问题
无具体缺陷描述。更新 Launcher 内嵌的 Kanzi 3D 渲染引擎库（3D 车模展示用）。

## 根因分析
不适用。本提交为纯二进制更新：`application/Launcher/libs/kanzi-release.aar` 由 396,827,325 字节更新为 400,668,356 字节（约 +3.7MB），无任何源码 diff，提交信息 [what]/[why]/[how] 均为占位文案。Kanzi aar 来自第三方渲染引擎团队，此类提交通常是同步外部团队的修复或版本升级。

## 关键代码修改
改动文件：application/Launcher/libs/kanzi-release.aar（二进制更新，396.8MB → 400.7MB）

## 为什么能修复
不适用。内容变化封装在 aar 内部，本仓库侧无法审计其内部变更；测试范围标注"3D车模"，需依赖 Kanzi 团队的变更说明与 3D 车模功能回归验证。

## 复盘与经验
- 大体积二进制 aar 直接入 git 会使仓库持续膨胀（单文件已近 400MB），建议评估 LFS 或制品库管理。
- 依赖方提交无说明的二进制更新时，应在提交信息中带上上游版本号/变更单号，否则事后无法追溯该版本对应的内容。
