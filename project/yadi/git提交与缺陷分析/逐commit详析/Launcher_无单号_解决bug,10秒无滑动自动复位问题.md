# 无单号 · Launcher 座椅调节/萌宠/动画等一批问题修复（aar 二进制更新）

- **提交**：`d1d81f91` | 2026-09-11 | liqingqing | Launcher | bugfix（纯二进制）
- **缺陷库**：未关联单号（标题含 `SIR-***` 脱敏占位）

## 类型标注
**纯 aar 更新提交**：唯一改动为 `application/Launcher/libs/kanzi-release.aar`（二进制更新，481953864 → 396827325 字节），无源码 diff 可剖析。

## 说明
提交标题列出该 aar 承载的一批修复：座椅调节 10 秒无滑动自动复位、座椅调节 UI 颜色、萌宠在五指抓屏下切 P 档会消失一段时间、动画效果调整、"调节一个滑杆另一个消失"的处理。这些问题均在 Kanzi 3D 座椅/萌宠渲染层内实现，源码不在本仓库，无法从 diff 验证具体修复机制，不做强行剖析。

## 复盘与经验
- 以 aar 形式交付的功能模块，bug 修复不产生可读 diff，缺陷复盘只能依赖提交说明；建议在提交信息中逐条写明修复机制而非只罗列现象。
- 单个 aar 承载多个不相关缺陷修复，回滚粒度差（回滚一个会带走全部）；理想做法是按问题拆分交付。
