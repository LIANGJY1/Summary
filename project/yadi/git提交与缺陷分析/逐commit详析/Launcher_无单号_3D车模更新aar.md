# 无单号 · 3D 车模 kanzi-release.aar 更新

- **提交**：`b3819b67` | 2026-07-07 | liqingqing | Launcher | **二进制依赖更新类提交**
- **缺陷库**：未关联单号

## 问题
3D 车模（kanzi 引擎侧）存在若干渲染/交互问题，需更新车模 aar 修复。

## 根因分析
车模渲染逻辑封装在 `kanzi-release.aar` 中，由车模团队单独迭代；本仓库仅负责集成。问题细节不在本仓库代码内，无法从 diff 剖析。

## 关键代码修改
改动文件：
- `application/Launcher/libs/kanzi-release.aar`（二进制更新，343MB → 237MB）

无源码 diff。

## 为什么能修复
直接替换 kanzi 车模二进制，修复内容包含在 aar 内部（提交消息仅注明"3D车模修复问题"）。需注意 aar 体积从 343MB 降到 237MB（资源瘦身或裁剪），集成后 3D 车模相关功能（车模显示、座椅/灯光/充电状态联动）均需回归。

## 复盘与经验
- **aar 黑盒更新必须配套版本说明**：本提交仅说"修复问题"，未列修复点与 aar 版本号，后续追溯 SIR 单与 aar 版本的对应关系困难。
- 仓库内 300MB 级二进制依赖对 git 体积压力极大，宜考虑制品库/maven 坐标管理而非直接入库。
