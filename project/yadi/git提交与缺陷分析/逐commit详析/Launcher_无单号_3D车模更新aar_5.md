# 无单号 · 3D 车模更新 aar（二进制更新，不展开剖析）

- **提交**：`5cb3a951` | 2026-08-28 | liqingqing | Launcher | **纯 aar 更新（特殊情况：stat + 说明）**
- **缺陷库**：未关联单号（ids 为空）

## 改动说明
```diff
--- application/Launcher/libs/kanzi-release.aar（二进制更新）
-Bin 239505066 -> 257272333 bytes
```
仅替换 Launcher 内的 3D 车模 Kanzi 渲染库 `kanzi-release.aar`（约 239.5MB → 257.3MB），无任何源码改动。此类提交的修复内容封装在二进制内部，无法从 diff 佐证根因，按约定不做强行剖析；测试范围为车模显示。

## 复盘经验
- aar/二进制提交务必在标题注明来源版本或关联需求/缺陷，否则仓库历史中无法追溯"这次更新修了什么"。
- 单文件超 200MB 的二进制直接入 git 会持续膨胀仓库体积，建议配合 LFS 或制品库管理。
