# SIR-6246 · 预约开启首次上电默认为空（应为 22:00-8:00）
- **提交**：`f51e7e2a` | 2026-08-25 | sgh | Setting | bugfix（diff 与单据描述不符，见下）
- **缺陷库**：等级 C · 频次 必现-80%~100% · 状态 关闭 · 域 安全监控

## 问题
单据描述：哨兵/安全监控的"预约"功能首次上电时开始/结束时间为空，需求要求默认 22:00-8:00。

## 根因分析（含 diff 与元数据不符声明）
**本提交的实际 diff 与提交消息不符。** 提交消息写"[how]初始化设置开始结束时间"，但 diff 只改了 `SafetyMonitorFragment` 中"格式化存储设备"按钮：给 `btnFormat` 的点击补了 `showWarningDialog` 二次确认弹窗（title/content/confirmText/cancelText 均取自 format_storage 系列字符串），确认回调内仍是 `//TODO 接入格式化存储设备内部接口`。全 diff 无任何预约时间（22:00/8:00）初始化代码。经全库搜索，`SIR-6246` 仅命中本提交及其同 Change-Id 的兄弟提交 `f01ae701`（内容完全相同的分支移植），两者都不含预约时间初始化；推测提交消息误用/漏带真正的预约改动，预约默认值的修复未落在本次 diff 中。

## 关键代码修改
改动文件：application/Setting/src/main/java/com/yadea/setting/ui/fragment/SafetyMonitorFragment.kt
```diff
--- a/.../ui/fragment/SafetyMonitorFragment.kt
@@ 格式化存储设备
         mBinding.btnFormat.setOnFastClickListener {
-            log("format storage device click")
-            //TODO 接入格式化存储设备内部接口
+            showWarningDialog(
+                title = getString(R.string.format_storage_title),
+                content = getString(R.string.format_storage_content),
+                confirmText = getString(R.string.format_storage_action),
+                cancelText = getString(R.string.cancel)
+            ) {
+                //TODO 接入格式化存储设备内部接口
+                log("format storage device click")
+            }
         }
```

## 为什么能修复
就 diff 本身而言，它只是给格式化存储加了破坏性操作二次确认（防误触，属体验加固），并不能修复"预约默认时间为空"。SIR-6246 所述问题在本提交中未见对应代码，单据虽已关闭，修复证据缺失，无法确认"22:00-8:00 默认值"由哪个改动写入。

## 复盘与经验
- 提交消息与 diff 不一致是流程红灯：关闭缺陷前应能从提交历史中指认修复代码，否则回归验证缺少锚点。
- 大量时间花在"格式化确认弹窗"上而提交消息写预约初始化，疑似复制了错误模板；建议提交前 `git diff --stat` 自检一眼。
- 对"需求遗漏"类单据，修复应尽量收敛在初始化路径（如 GSetting 无值时写入默认 22:00-8:00），本批数据中该模式未在本提交体现，复盘存档以备追溯。
