# 无单号 · Revert Vlog 分支合并（dev_202606271231）

- **提交**：`5cb44514` | 2026-06-27 | liujinfeng | Vlog | other（Revert）
- **关联单**：无

## 需求/目标
回退两天前合入 main 的 Vlog 分支 dev_202606271231——该分支的首页改版（activity_home.xml 大改 + 三张背景图）未达预期，整体撤销。

## 实现结构
改动文件：Vlog 的 build.gradle、activity_home.xml（-117/+53）、三张 mdpi 背景图删除、values/values-en 文案删除。
注意 values-en 与 values 同步删除——回退保持了双语一致性。

## 关键代码
纯资源/布局回退，无逻辑改动。

## 复盘与要点
- 项目初期（6月27日，仓库建立第3天）就出现整分支回退，说明 Vlog 首页改版走了一条"先合入再评价"的路径。
- 好的一面：回退干净（含英文资源），没有留下半撤销状态；两天后 Vlog 以新的实现重新提交（见 98165d75 同日第二笔回退）。
- 教训：改版类需求建议先在个人分支出预览包评审，再合入主干；"合入后不满意再 revert"污染主干历史且增加所有下游集成分支的合并成本。
