# 萌宠需求与全局决策

> 唯一职责：全局决策 D／全局规则 G／术语／信号清单／跨条目开放点。
> 条目级结论见 [entries/](./entries/) 条目档案（目录见 [03-需求条目](./03-需求条目.md)）；竞合规则见 [02-音效动作冲突矩阵](./02-音效动作冲突矩阵.md)；代码方案见 [04-代码方案](./04-代码方案.md)。
> 2026-09-09 精简重构：编号登记索引已删，F 编号降级为条目档案内局部锚点（不再全局登记）；讨论日志与审查报告已删（历史在 git）。

## 1. 概述

萌宠是车机主界面上由 **Kanzi 实时渲染**的虚拟宠物角色，与 3D 车模共处同一 3D 场景（共享渲染画布 kanzi_surface），位于车模旁固定位置，主界面为核心展示区。Launcher 侧萌宠服务负责信号采集、动作仲裁与指令下发，是 VPA 的视觉反馈层。
职责红线：服务不关心显隐渲染细节与画面；显隐仅两个入口（G1 开关、G7 失败兜底）。

## 2. 全局决策（D）

- **D001 全新起步**：展车版本萌宠代码为 demo 废弃代码，这部分代码不要修改不要侵入不要使用就当不存在；设计可以适当参考；例外：音源监听（AudioSourceMonitor）、aiLit 语音链路属已实装基础设施，可复用能力。
- **D002 中断恢复统一走 setRenderStop/Start 记忆-恢复**：stop 记住当前动作，start 恢复并重新计时；状态型动作恢复前按实时信号校验重建，不机械还原。
- **D003 新旧两版共存 main，入口开关一键切换**：`Myapplication.USE_NEW_PET_SERVICE`（默认 false＝展车版）双闸生效——生命周期挂载二选一＋`KanziDataSourceManager` 的 `VPA.` 数据分流。两包互不引用；正式投入前 main 构建行为＝展车版。
- **D004 上电周期＝进程生命周期**：进程初始化即上电，进程重启即新周期；"每上电至多一次"类标记随进程内存，不持久化。
- **D005 前台判定统一走 Render，档位不参与萌宠判定**（2026-09-09 需求方勘误，**代码已实施同日**）：式样原文中所有档位判断（出场触发的 P 档、D/R 终止、循环/点击的"驻车桌面"前置等）均系"萌宠页面是否在前台"的误写。前台判定统一使用 Launcher 既有机制：**`setRenderStart()`＝萌宠页面在前台、`setRenderStop()`＝在后台**（既有仲裁点：`KanziDataSourceManager.checkAndPerformRenderState()`，按前台状态＋仪表形态∈{0,3}决定）。推论：①出场触发＝IviReady＋前台＋未插枪；②出场终止＝页面切后台（原 D/R 终止作废），回前台不恢复出场；③循环/点击前置＝前台；④档位信号与 GearValid 不再被萌宠需要（DEP-6 关闭）。实现：`GearChanged` 事件/档位订阅/gear 字段全部移除；`PetCore.onRender` 入口＋setRenderStart/Stop 处按入口开关透出；状态机默认前台（Launcher 开机即前台，首个生命周期沿对齐）。
- **D006 Launcher 对外 SDK 与服务统一入口**：`component:LauncherLib` 是在 `settings.gradle` 注册、采用 `com.android.library` 插件的 Android Library Gradle 模块，产物为 AAR；它不是 Java Library 或普通文件夹。Launcher 进程提供根包中的全局 Android `LauncherService`，固定返回 `ILauncherService` 根 Binder，再由 `getPetService()` 取得 pet 子接口。Android 对同一 Service 的多个绑定复用首次 `onBind()` 返回的 Binder，不能按 action 返回不同接口。SDK 负责 `bindService`、回调注册、断线重连与连接前事件暂存；`LauncherService` 只负责分发、生命周期和共同鉴权；`PetRecommendationManager` 负责 pet 事件翻译与回传，萌宠状态机、竞合、Kanzi 与音效仍由 Launcher 内部负责。新增业务在同一 AAR 中增加独立能力域和子接口，不把业务方法堆进根 AIDL。天气输入链路归 SRS_011，使用天气方提供的独立 SDK。

## 3. 全局规则（G）

| # | 规则 |
|---|---|
| G1 | 显隐与启停由单一开关 `ai_pet_switch` 决定；关→Kanzi 隐藏；缓存兜底；形象不参与启停 |
| G2 | 竞合唯一权威＝02 冲突矩阵（**2026-09-16 定版 PP0 锁版**：含多动作优先级顺序与"连续动作被打断不恢复"性能建议）；修订走 02 修订记录 |
| G3 | 中断恢复与竞合适用边界按 D002：前台未落位→竞合照常；不在前台→记忆恢复＋回前台实时重建 |
| G4 | 归宿：除皮肤叠加外，一切动作结束回常驻待机循环 |
| G5 | 上电多事件不设全局顺序表，按到达顺序＋矩阵两两关系推算；皮肤即达即叠加不排队 |
| G6 | 音效通用机制：服务播、等动作开始回调、1000ms 超时弃播不取消动作 |
| G7 | 保活不做；发送失败重试 1~2 次后调隐藏接口兜底 |

## 4. 术语（仅保留易混项）

- **萌宠形象开关 / 萌宠形象**：决定显隐的单一设置项（避免叫"萌宠开关"）／设置页可选外观，只决定呈现。
- **车控车设 / 萌宠 App / PPX 阶段**：设置数据现由 Setting 应用提供／未来承接萌宠设置的独立应用（尚未存在）／两者之间的过渡期。
- **出场动作**：上电一次性迎接动画，播完转常驻待机（避免：开场动作）。
- **常驻待机 / 常驻动作 / 随机动作**：默认持续状态（避免：常驻态）／两次随机之间的基础待机动作（5~10s）／循环中抽取的短微动作（无音效）。
- **驻车桌面**：~~P 档驻车状态下的桌面形态~~ **作废（D005）**——原以 P 档代指"萌宠页面在前台"，统一改用后者表述。
- **共享渲染画布**（kanzi_surface）：主界面唯一 Kanzi 驱动渲染区域（避免：双 surface）。
- **点击热区**：随萌宠屏幕位置动态变化的命中区域，Kanzi 判定。
- **动作开始回调 / 动作完成回调**：Kanzi→服务"已开始"/"已播完"事件；**动作终止信号**：服务→Kanzi"立即停止"指令。

## 5. 信号与接口清单

| 类别 | 项 | 状态 |
|---|---|---|
| 页面前后台 | **Render 沿＝Launcher 既有渲染仲裁**：`KanziDataSourceManager.checkAndPerformRenderState()`（前台状态＋仪表形态∈{0,3}）→ `setRenderStart()`/`setRenderStop()` 处按入口开关调 `PetCore.onRender`（D005） | ✅ 已实施 |
| 设置存储 | Settings.Global `ai_pet_switch`/`ai_pet_choose`＋ContentObserver＋本地缓存（另：`SWITCH_FORCE_ON` 临时代码强制常开，Setting 就绪后撤销） | ✅ 已实装 |
| 车辆信号 | AC 枪／DC 枪：复用 VehicleService 订阅链，订阅键＝能量族别名 4010/4011（⚠️ Carlib 事件按映射表首匹配键重贴标签，原始键收不到事件） | ✅ 已实施 |
| 车辆信号 | ~~档位／档位有效性~~ **已移除（D005，2026-09-09）**：GearChanged 事件、档位订阅、gear 字段全部删除 | ❌ 已移除 |
| 车辆信号 | 电量：原定 `BMS_RealSOC`，**2026-09-16 实际接入 `BMS_DISPSOC` 显示电量**（`CarPropertyIds.ENERGY_DISPSOC`=4019，复用 VehicleService 订阅链，Float→Int 截断后判定） | ✅ 已实施 |
| 上下电时间（SRS_007） | 记录链：萌宠侧 10min 周期存储＋SCREEN_OFF（进入 STR）下电沿尽力补写；判定链：上电沿（冷启/SCREEN_ON STR 唤醒，每沿一次）直接取系统时间判定（2026-09-21 修订，不依赖 MCU 信号），STR 对用户算下电 | 🔶 Launcher 侧已编码（域判定器 PetIdleClock）；待新版入口启用、真机验证 |
| 车机就绪 | IviCommManager `IVI_Ready_Status` 握手确认（经 `VehicleService.addOnIviReadyListener`） | ✅ 已实施 |
| 音源监听 | AudioSourceMonitor（焦点层＋播放层，蓝牙＋网易云双源） | ✅ 已实装 |
| 语音 | aiLit SDK（onStart/onEnd 映射唤醒/结束） | ✅ 已实装 |
| 天气（SRS_011） | Launcher 萌宠侧通过天气方的 `WeatherApiClient` 接收 `WeatherInfoBean.weatherCode` 并映射雨/雪动作；接入与码表见 [SRS_011](./entries/SRS_011.md)，不经过智能推荐或 LauncherLib | 🔶 Launcher 侧已编码；待新版入口启用/真机联调 |
| 智能推荐（SRS_010/012） | 推荐侧判定生日／节日，经 `LauncherLib` 客户端绑定全局 `LauncherService` 的 pet 入口；底层 AIDL 见 [06](./06-智能推荐接口设计.md)，待双方联签 | 🔶 Launcher 侧已编码；待新版入口启用/联签/真机联调 |
| Kanzi 接口 | **正式 VPA_ 键族（2026-09-16 对齐）**：VPA_Action（含持续型开始/结束码 70/71、80/81、90/91、100/101）、VPA_Switch、VPA_Clicked、VPA_Out_Action_PlayStatus、VPA_Out_Action_StartPlayIndex（动作开始回调）；VPA_Festival 已接节日皮肤属性写入，VPA_Style 仍待形象阶段 | 🔶 属性写入已编码，节日效果待真机确认 |

## 6. 跨条目开放点

| 编号 | 内容 | 状态 |
|---|---|---|
| TD-17 | 竞合矩阵整体完善：状态重算点（TD-11）、开关重开初始态（TD-13 暂行：直接常驻不补出场）已随定版＋表驱动重构收口（2026-09-16）；**残余＝TD-12 一次性彩蛋被拒丢失**，阶段 7 编码未关闭该异常策略，待推荐方联签（SRS_007 新需求已于 2026-09-20 澄清定稿） | 大部收口 |
| DEP-K1 | Kanzi 正式协议包（码表/回调名/指令名，含 hide、皮肤、setRender 系列） | 待与 Kanzi 团队定稿 |
| DEP-S1 | 正式音效资源包（位置/提供方/格式）；一期 Launcher `res/raw` 占位 | 待资源方认领 |
| — | 矩阵内未决 QA：常驻待机完成信号、语音唤醒状态接口、Mediasession 描画时序、HMI 切换计时（~~48h 底层记录~~ 已随 SRS_007 2026-09-20 上层计时口径收口） | 随 02 维护 |
