# SRS_003 — 随机动作

> ✅ 已定 2026-08-27　｜　**代码已实施（阶段2，2026-09-08）**：完成回调驱动循环（随机→常驻＋5~10s 计时→随机）；洗牌不重复（固定种子注入）；非 P／前台丢失→暂停＋重进常驻恢复（D002）
> 本文件是本条目事实／问答／开放点的唯一权威。全局决策／规则／术语／信号清单见 [01-需求与决策](../01-需求与决策.md)（中断恢复见 D002）；竞合规则见 [02-音效动作冲突矩阵](../02-音效动作冲突矩阵.md)；不确定点的代码落点见 [04-代码方案](../04-代码方案.md) §6 收敛表。

## 事实

- 前置：**萌宠页面在前台**（Render started，D005 勘误；原写"驻车桌面（P 档）"）、萌宠前台常驻态；触发输入＝空闲 ≥5 秒且 ≤10 秒
- F17 同屏融合钉死：与车模同一 3D 场景、车模旁固定位置；主界面为核心展示区
- F18 动作池 4 个（3 随机＋1 常驻）交替、随机无音效；节奏示例“随机→常驻→随机→…”
- 挂起/恢复信号＝Launcher 既有 `setRenderStop()`/`setRenderStart()`（D002 记忆-恢复的载体，D005）

## 问答

1. **随机去重**

   QA: 洗牌不重复（相邻不同）

2. **交替节奏与打断恢复**

   QA: 完成回调驱动闭环——随机→完成回调→常驻＋5~10s 计时→随机；打断恢复＝页面切后台挂起（Render stop），回前台重进常驻＋重新计时（Render start）

3. **5-10s 参数**

   QA: 常驻时长＝触发间隔，同一可配置参数（默认值 TBD-4）

4. **非理想状态行为**

   QA: 全局 setRenderStop/Start 记忆-恢复（立 D002，F20）；服务不关心显隐与画面

5. **动作池扩展**

   QA: 可配置动作编号列表，增减只改配置

6. **首次循环起点**

   QA: 落位完成事件；位移动画由 Kanzi 内部消化

## 开放点

- TBD-4 常驻动作时长／触发间隔默认值（区间已定 5~10s，同一可配置参数）
